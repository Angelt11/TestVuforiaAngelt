notes, are being show with a canvas renderer
change order in layers
we tested changing layers but it didnt work
possible solution define a canvas for each shown object(we dont wnat this because its a lot of work)
possible solution 2 make it into the code(we need to check how to do it how to do it with canvas renderer)